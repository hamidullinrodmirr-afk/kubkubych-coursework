# Gap-анализ учебного проекта для лабораторных

Дата анализа: 16.09.2026. Проект: «КубКубыч» — интернет-магазин LEGO-наборов.

## Границы и достоверность

Подтверждённые факты ниже основаны на статической проверке репозитория: исходного кода, шаблонов, конфигурации, README и истории Git. Исходники не изменялись.

Требования сверены с заданиями 1–14 из раздела «Весна 2026» курса «Веб-технологии 241-671, 241-672» в СДО 15.09.2026. На момент просмотра в СДО у каждого задания отображалось «Ответы на задание еще не представлены» и «Не оценено». Это статус заданий в СДО, а не вывод о качестве кода.

## Краткая оценка проекта

Проект является развёрнутым Django-приложением, а не статичной вёрсткой. Реализованы серверный HTML-интерфейс и REST API, аутентификация и роли, каталог, фильтрация, корзина, заказ, отзывы, избранное, административная аналитика, Docker Compose и фоновые задачи Celery. По масштабу он потенциально может закрывать несколько лабораторных, если в них допускается Django и совпадают предметные требования.

Подтверждённые артефакты:

| Направление | Что найдено | Доказательство |
| --- | --- | --- |
| HTML/CSS и адаптивность | Базовый шаблон, русская локализация, viewport, адаптивные media queries, формы, каталог, страницы заказа и профиля | `templates/base.html`, `static/css/style.css` |
| Клиентский JavaScript | Работа с API, динамический каталог, корзина, избранное, уведомления, клиентские сценарии форм | `static/js/api.js`, `static/js/app.js`, встроенные скрипты в `templates/` |
| Бэкенд | Django 5, Django REST Framework, модели и миграции по доменам users/products/cart/orders/reviews | `requirements.txt`, приложения проекта, `*/migrations/` |
| Авторизация и права | JWT, регистрация, профиль, OAuth Google/VK, роли покупателя и администратора, объектные permissions | `users/`, `kubkubych/settings.py` |
| Бизнес-логика | Контроль остатка, атомарное создание заказа, статусы, возврат остатка при отмене, ограничения на отзывы | `orders/services.py`, `orders/models.py`, `cart/`, `product_reviews/` |
| API и документация | Роуты API, Postman-коллекция, описание API | `kubkubych/urls.py`, `docs/API_KUBKUBYCH.md`, `postman/KubKubych_API.postman_collection.json` |
| Развёртывание | Dockerfile, Compose: web, PostgreSQL, Redis, Celery, Beat, Mailhog, nginx | `Dockerfile`, `docker-compose.yml`, `nginx/default.conf` |
| Автотесты | Наборы Django/DRF-тестов для пользователей, каталога, корзины, заказов, отзывов и аналитики | `users/tests.py`, `products/tests.py`, `cart/tests.py`, `orders/tests.py`, `product_reviews/tests.py`, `analytics/tests.py` |

## Diff-матрица для сопоставления с лабораторными

Это вспомогательная сводка реализации. Точное сопоставление с требованиями заданий 1–14 приведено в следующем разделе.

| Типовой критерий лабораторной | Статус в проекте | Что уже есть | Что может не хватать для зачёта |
| --- | --- | --- | --- |
| Многостраничная HTML-вёрстка | Есть | Главная, каталог, карточка, корзина, checkout, заказы, профиль, информационные страницы | TBD: требуемые преподавателем страницы, структура отчёта и способ сдачи |
| CSS-оформление и адаптивность | Есть | Собственные стили, сетки и брейкпоинты 1024/860/768/480 px | TBD: требования к методологии/фреймворку, макету, browser support |
| Формы и валидация | Частично подтверждено | HTML `required`, типы полей, клиентская обработка; серверная валидация serializers/models | TBD: список обязательных полей и сценариев из задания; отдельные accessibility-требования |
| JavaScript и DOM | Есть | Рендер карточек, фильтрация, async-запросы, управление корзиной/избранным, уведомления | TBD: обязательно ли применение конкретных API, модулей, localStorage или сторонней библиотеки |
| Работа с HTTP/API | Есть | REST endpoints, JWT, документация и Postman-коллекция | TBD: требуемые методы/контракт, формат отчёта или скриншоты запросов |
| Серверная часть и БД | Есть | Django ORM, миграции, SQLite/PostgreSQL, CRUD доменных сущностей | TBD: требуемая СУБД, ER-диаграмма, нормализация и список сущностей |
| Регистрация и авторизация | Есть | Регистрация, JWT, профиль, logout, OAuth-маршруты, роли | TBD: требуется ли именно сессионная авторизация или отдельный сценарий восстановления пароля |
| Разграничение доступа | Есть | `IsAdmin`, владелец заказа/корзины/отзыва, фильтрация queryset | TBD: набор проверяемых ролей и демонстрационные сценарии |
| Асинхронность/фоновые задачи | Есть | Celery worker/beat, почтовые и отчётные задачи | TBD: если лабораторная требует конкретный механизм, очередь или UI-демонстрацию |
| Тестирование | Частично подтверждено | Тестовые модули присутствуют, README заявляет 53 сценария | Не подтверждён запуск: локальный `venv` ссылается на отсутствующий Python 3.11; результаты тестов нельзя считать проверенными |
| Контейнеризация | Есть | Dockerfile, Compose, nginx, PostgreSQL, Redis | TBD: требования к production-конфигурации, публикации и README для проверки |
| Доступность | Частично | `lang`, viewport, `label` у части полей, `alt` у динамических изображений, отдельные `aria-label` | Нужен аудит клавиатурной навигации, видимого фокуса, имён всех кнопок и контрастности; часть кнопок использует только символы/`title` |
| Безопасность конфигурации | Частично | `.env` исключён из Git, секреты OAuth берутся из окружения | Для production нельзя оставлять `DEBUG=True` по умолчанию, dev `SECRET_KEY`, `send_default_pii=True`; пароль PostgreSQL прописан в Compose. Нужны требования лабораторной, чтобы оценить критичность для сдачи |
| Отчёт/пояснительная записка | Частично | README, API-документация, Postman-коллекция, 14 Markdown-отчётов в `LAB/`, ER-схема и таблица соответствия | Не подготовлены реальные скриншоты, титульные листы и оформленный документ по шаблону преподавателя |

## Сопоставление с заданиями «Весна 2026»

Статусы: **выполнено в коде** — реализация найдена; **частично** — найдена лишь часть обязательных пунктов; **не найдено** — в репозитории отсутствует код или требуемый артефакт. Статус не означает автоматического зачёта: для большинства заданий требуется отдельный отчёт и загрузка в СДО.

| Задание | Статус | Подтверждено в проекте | Не хватает для сдачи / нельзя подтвердить |
| --- | --- | --- | --- |
| 1. Проектирование БД | Частично | 9 моделей, роли, 2 аналога, описание сущностей и ER-схема подготовлены в `LAB/lab-01-database.md`; есть M:N через `OrderItem` и `Favorite` | Вставить реальные скриншоты аналогов и при необходимости экспортировать диаграмму в формат преподавателя. |
| 2. Административная часть Django | Частично | `date_hierarchy`, `list_display_links`, `raw_id_fields`, `readonly_fields`, inline, действия, импорт/экспорт и PDF реализованы; отчёт — `LAB/lab-02-admin.md` | Нужны реальные скриншоты; `filter_horizontal` применим только к прямому M:N и не используется. |
| 3. Django 1 | Частично | `reverse`, `get_object_or_404`, пагинация с `try/except`, пользовательский менеджер, фильтры и аннотации добавлены; отчёт — `LAB/lab-03-django-1.md` | Не найдены два собственных template filter и три template tag; требуется ручная демонстрация и ответы на вопросы задания. |
| 4. Django 2 | Частично | ModelForm, `clean_article`, `save(commit=True)`, files, CRUD через HTML, M:N through, `select_related`, `prefetch_related` и debug toolbar реализованы | Нужны реальные скриншоты формы, CRUD и панели запросов. |
| 5. Django 3 | Частично | Метаданные моделей, ImageField, redirect, inline и методы моделей подтверждены; отчёт — `LAB/lab-05-django-3.md` | Нет отдельного прямого сценария с `request.session`; показать вход и доступ к служебной странице либо добавить явный код сессии, если это обязательно. |
| 6. Django 4 | Частично | `FileField`, `URLField`, PDF action, media, локальный cache backend и отчёт реализованы | Требуется фактическая демонстрация файла/PDF; отдельное кэшируемое представление не найдено. |
| 7. Django 5 | Частично | Предметный M:N, QuerySet-операции и свойства `ProductForm` реализованы; отчёт — `LAB/lab-07-django-5.md` | Не подтверждён отдельный пример `contains`; нужны скриншоты формы и ответов на вопросы. |
| 8. Консольный редактор Vim | Частично | Подготовлен честный шаблон ручного отчёта `LAB/lab-08-vim.md` | Выполнить операции в Vim и вставить реальные скриншоты. |
| 9. Продвинутый Git | Частично | Подготовлен журнал для операций из PDF в `LAB/lab-09-git.md` | Сверить PDF, выполнить операции и заполнить журнал реальными командами/скриншотами. |
| 10. Фронтенд | Частично | `@media print`, базовые accessibility-атрибуты и отчёт `LAB/lab-10-frontend.md` есть | Нужна ручная проверка в браузере и скриншоты фокуса/предпросмотра печати. |
| 11. Главная страница | Частично | Подборки, поиск на главной, аннотации и бизнес-логика заказа задокументированы | Проверить количество записей во всех таблицах и собрать скриншоты с заполненной базой. |
| 12. Страница конкретного объекта | Частично | Детальная страница по slug и HTML CRUD `/manage/products/` реализованы и описаны | Нужны реальные скриншоты и проверка прав доступа. |
| 13. Дополнительные библиотеки для админки | Частично | `django-simple-history` для Product/Order, `django-import-export` для Category/Product и отчёт подключены | Нужна демонстрация истории и выгрузки на работающей базе. |
| 14. Документация по проекту | Частично | Каркас пояснительной записки и ссылки на исходные материалы — `LAB/lab-14-documentation.md` | Нужно собрать оформленный документ по шаблону, титульный лист, автооглавление и реальные скриншоты. |

## Минимальный приоритет доработок

1. Собрать реальные скриншоты и титульные листы для всех отчётов.
2. Выполнить вручную Vim и Git по PDF задания, заполнить журналы фактическими командами.
3. Добавить template tags/filters и при необходимости отдельный пример `contains`.
4. Проверить наполнение каждой используемой таблицы и ручные сценарии доступа/печати.

## Выявленные разрывы, не зависящие от текста лабораторных

### Блокирующие для доказательства работоспособности

1. `manage.py check` успешно проходит в доступном рабочем runtime; полный набор тестов в рамках этого анализа не получил итогового статуса и требует отдельного запуска.
2. Локальное виртуальное окружение в рабочей директории по-прежнему ссылается на недоступный интерпретатор Python 3.11. Для воспроизводимой проверки его нужно пересоздать по README.

### Недостающие материалы для сдачи

1. Сопоставление «лабораторная → критерий → артефакт → сценарий» добавлено в `LAB/`; его нужно дополнить фактическими скриншотами.
2. ER-диаграмма добавлена в отчёт ЛР-1. Схема архитектуры не создавалась, так как в явно прочитанном задании не была обязательной.
3. Не найдены результаты фактического запуска: скриншоты UI, API-запросов, тестов и Docker Compose. Наличие кода не заменяет доказательство выполнения.

### Технические замечания для ручной проверки

1. `kubkubych/settings.py:13` задаёт development-ключ по умолчанию, а `kubkubych/settings.py:15` — `DEBUG=True`. Это допустимо только для локальной разработки; настройки нужно явно разделить для публичного развёртывания.
2. `kubkubych/settings.py:29` включает `send_default_pii=True` при подключённом Sentry. Перед публикацией нужно подтвердить, допустима ли передача персональных данных и есть ли согласие/политика обработки.
3. `docker-compose.yml:11` содержит пароль PostgreSQL прямо в Compose. Это не секрет для учебного локального стенда, но не должно переноситься в публичный репозиторий или развёртывание без переменной окружения.
4. `README.md:67-71` раскрывает демонстрационные учётные данные. Для локальной демонстрации это может быть приемлемо; для опубликованного доступного стенда нужно заменить пароль, ограничить доступ или описать безопасный bootstrap.
5. В клиентском коде есть inline-обработчики `onclick`; это не ошибка само по себе, но может не соответствовать требованиям лабораторной к разделению HTML/JS. Например: `templates/cart/index.html`, `templates/catalog/detail.html`, `static/js/app.js`.

## Открытые вопросы

1. Допускается ли засчитать один учебный проект за несколько лабораторных, и требуется ли отдельный коммит/папка/отчёт на каждую?
2. Нужна ли публичная ссылка на развёрнутый проект, или достаточно локального запуска/Docker?
3. Достаточны ли модель-связка OrderItem и таблицы Favorite/CartItem как демонстрация M:N для задания 1, или преподаватель ожидает именно `ManyToManyField(through=...)` в коде?
4. Какая версия Python должна использоваться в учебной среде и можно ли пересоздать локальное виртуальное окружение?

## Следующий шаг

Согласовать, допускается ли зачёт общей реализации вместо отдельных артефактов. Затем закрывать доработки по разделу «Минимальный приоритет доработок» и подготовить отдельные отчёты для загрузки в СДО. Без отчётов и технических пунктов из задач 6, 8, 10 и 13 работу нельзя считать готовой к полной сдаче.
